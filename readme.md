# Pluton Diagram Generator CLI

A command-line tool to generate diagrams from JSON files. Supports state machine diagrams with transitions, labels, and custom styling.

## Installation

### Global Installation (Recommended)

```bash
npm install -g pluton-diagram-cli
